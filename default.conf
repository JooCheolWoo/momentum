server {
    listen 10900;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location = /sitemap.xml {
        root /usr/share/nginx/html;
    }

    location = /ads.txt {
        root /usr/share/nginx/html;
    }    
}